# Direct dependencies for local development.
# The full pipeline runs on Databricks (Spark provided by the runtime);
# these pins cover local generators, notebooks, and the Streamlit dashboard.

# Spark (local dev / notebook parity with Databricks Runtime)
pyspark==3.5.0

# Data generation + local processing
pandas==3.0.3
numpy==2.4.6
pyarrow==24.0.0          # parquet backend for pandas read/write
Faker==40.21.0

# Dashboard
streamlit==1.58.0
plotly==6.8.0

# HTTP (OpenFEMA API pulls in warmup/exploration)
requests==2.34.2
